<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
</head>
<body>
    <h1>동적 폼 실습</h1>
    <hr>
    <h2>실습 1.axios get 회원가입</h2>

    <form name = 'login1'>
        <label for="name">이름: </label><input type="text" id="name" name="name" />
    <fieldset>
        <legend>성별</legend>
        <input type="radio" name="gender" id="man" value="man" />
        <label for="teacher">남자</label>
        <input type="radio" name="gender" id="woman" value="woman" />
        <label for="student">여자</label>
    </fieldset>
    <fieldset>
        <legend>생년월일</legend>
        <select name="birthyear" id="year" >
            <% for(var i=1950; i<2023; i++){ %>
                <option value="<%= i  %>">
                    <%= i %>
                </option>
            <% } %>
        </select>
        <label for ="year">년</label>
        <select name="birthmonth" id="month" >
            <% for(var i=1; i<13; i++){ %>
                <option value="<%= i  %>">
                    <%= i %>
                </option>
            <% } %>
        </select>
        <label for ="month">월</label>
        <select name="birthday" id="day" >
            <% for(var i=1; i<32; i++){ %>
                <option value="<%= i  %>">
                    <%= i %>
                </option>
            <% } %>
        </select>
        <label for ="day">일</label>        
    </fieldset>
    <fieldset>
        <legend>좋아하는 음식</legend>
        <input type="checkbox" id="gimbap" value="gimbap" name="favoritefood" />
        <label for="gimbap">김밥</label>
        <input type="checkbox" id="pasta" value="pasta" name="favoritefood" />
        <label for="pasta">파스타</label>
        <input type="checkbox" id="pizza" value="pizza" name ="favoritefood" />
        <label for="pizza">피자</label>
        <input type="checkbox" id="chicken" value="chicken" name="favoritefood" />
        <label for="chicken">치킨</label>
    </fieldset>
    </br>
    <button type="button" onclick="axiosGet();">회원가입</button>
    
    <div class="result"></div>
    <hr>
    
    <h2>실습2. axios post 로그인</h2>

    <label for="username">아이디</label>
        <input type="text" id="id" name="id" />
        <label for="password">비밀번호</label>
        <input type="password" id="pw" name="pw" />
        <button type ="button" onclick="axiosPost();">로그인</button>
    </form>
    <div class="login-result"></div>
    <script>

        const resultBox = document.querySelector('.result');
        const loginResult = document.querySelector('.login-result')

        async function axiosGet(){
            console.log('click axiosGet btn!!')

            const form = document.forms['login1'];
            
         
            const formInfo = {
                name: form.name.value,
            };

            try {
                const response = await axios({
                url: 'axios',
                method: 'GET',
                params: formInfo 
                })

                console.log(response)
                console.log(response.data)

                resultBox.textContent = `${response.data.name}님 환영합니다! 가입이 완료되었습니다!'😁`;
                resultBox.style.color = 'blue';
            } catch (error) {
            console.log('Error!', error.message)
            }
        } 
         
        async function clickRegister() {
      console.log('click register btn!')

      const form = document.forms['register']
      const formInfo = {
        username: form.username.value,
      }

      // sol1) promise then() 
      // axios({
      //   url: '/practice1',
      //   method: 'GET',
      //   params: formInfo
      // }).then(function(response) {
      //   console.log(response)
      //   console.log(response.data) // { username: 'xxx'}
      //   registerResult.textContent = `${response.data.username}님 환영합니다! 가입이 완료되었습니다`
      //   registerResult.classList.add('success')
      // })

      // sol2) async/await 
      const response = await axios({
        url: '/practice1',
        method: 'GET',
        params: formInfo
      })
      console.log(response)
      console.log(response.data) // { username: 'xxx'}
      registerResult.textContent = `${response.data.username}님 환영합니다! 가입이 완료되었습니다`
      registerResult.classList.add('success')
    }

    function clickLogin() {
      console.log('click login btn!')

      const form = document.forms['login'];
      const formInfo = {
        userId: form.userId.value,
        userPw: form.userPw.value
      }

      axios({
        url: '/practice2',
        method: 'POST',
        data: formInfo
      }).then(function(response) {
        console.log(response)
        console.log(response.data)

        if (response.data.isLogin) {
          // 로그인 성공
          // response.data => {isLogin: true, userInfo: {…}}
          // => id님 로그인 성공 메세지
          loginResult.textContent = `${response.data.userInfo.userId}님 로그인 성공!!`
          loginResult.classList.remove('error')
          loginResult.classList.add('success')
        } else {
          // 로그인 실패 
          // response.data => {isLogin: false}
          // => 아이디 또는 패스워드 오류 메세지
          loginResult.textContent = `아이디 또는 패스워드 오류!!`
          loginResult.classList.remove('success')
          loginResult.classList.add('error')
        }
      })
    }
    </script>
</body>
</html>